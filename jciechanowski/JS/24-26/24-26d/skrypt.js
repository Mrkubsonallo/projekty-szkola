function powiekszenie(elem)
{
    elem.style.border="dashed";
    elem.style.width="400px";
}
function pomniejszenie(elem)
{
    elem.style.removeProperty("width");
    elem.style.removeProperty("border");
}